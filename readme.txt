


Tổng:
+ thiếu hiệu ứng animation trong các element
+ thiếu phần responsive 
+ thiếu code hỗ trợ css cho các trình duyệt ( coccoc, safari... )
+ phần hiển thị xem nhanh của từng sản phẩm: thanh ảnh hàng dưới, 
+ thiếu các sự kiện click + scroll trong website ( phần js + jquery ) 
+ thiếu phần nhúng facebook ở cuối phần sản phẩm ( trong khối màu chì)=>>> đã fix cần xem lại code
+ thiếu khối phần tử 2 bên website=>> ok
+ thiếu đường link liên kết bên ngoài website ( fb, yt, gg ...) =>> chưa xong
+ thiếu hiệu ứng cho 8 phần tử nằm dưới phần banner slide chuyển ảnh
+ thiếu thanh chạy theo kích thước trên phần banner chuyển slide lớn: =>> đã thêm. thiếu hiệu ứng pause( không set lại time ) cho slide khi hover


update: + nút next + prev của slide lớn: cần xử lí: chỉ hiện lên khi hover vào slide.


Bug:
+ trong phần DANH MỤC SẢN PHẨM : chưa xử lí phần tử bị tràn ra ngoài	=>> đã fix
+ hiệu ứng click chuột chuyển slide của mấy khối slide chuyển ảnh nhỏ ở cuối còn lỗi =>> đã fix



update:	+ trong phần xem sản phẩm css chưa chuẩn kích thước, cần css lại cho giống với trang của thầy.

k biết còn nữa k :v, chừ chưa tìm ra haha




+ chuyển slide ảnh trong phần xem sản phẩm ( phần slide ảnh nhỏ cuối)
+ chiều cao của phần tử bên trong banner left
+ hiệu ứng sau khi hết hover của scroll left có vấn đề. có thời gian thì fix tiếp

+ hiệu ưngs hover trên thanh menu: cần làm chậm hơn khi hover( thiếu animation)
+ thiếu animaion khi hover vào các sản phẩm


Bug update:

+ ở chế độ tablet thì title name của các khối sản phẩm bị tràn ra ngoài ( lỗi ni fix dc mà chưa có thời gian, thầy ko gắt cái ni :v)








